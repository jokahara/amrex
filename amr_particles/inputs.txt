
# PROBLEM SIZE & GEOMETRY
geometry.is_periodic =  1  1  0
geometry.coord_sys   =  0       # 0 => cartesian
geometry.prob_lo     =  0.0  0.0  0.0 
geometry.prob_hi     =  10.0  10.0  1.0
amr.n_cell           =  10  10  1

# default values
n_comp               =  1     # number of components
n_grow               =  1     # number of ghost cells around fab array

# VERBOSITY
amr.v               = 1       # verbosity in Amr

# REFINEMENT
amr.max_level       = 1       # maximum level number allowed ( >= 0 )
amr.ref_ratio       = 2 2 2 2 # refinement ratio (for each level)
amr.ref_dir         = 1 1 0   # which directions to refine (for example, in 2D)

amr.max_grid_size   = 16      
# amr.max_grid_size_x = 16
amr.blocking_factor_x = 1     # each grid is divisible by blocking_factor
amr.blocking_factor_y = 1     # must by either 1 or factor of 2
amr.blocking_factor_z = 1       

amr.regrid_int      = 2       # how often to regrid
amr.n_error_buf     = 0       # buffer cells around each tagged cell (0 is dangerous)

amr.loadbalance_with_workestimates = 0

# DistributionMapping.strategy = KNAPSACK